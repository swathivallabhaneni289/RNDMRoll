# Requirements (from PRD sources)

No entries — no PRD documents in ingest set.

The ingest set for this synthesis run contained only DOC-type sources (see `decisions.md` for the list). No formal requirements with acceptance criteria were extracted. Candidate feature/requirement material exists in `context.md` (Core Mechanics, Feature Backlog, Open Questions) sourced from README.md and docs/original-concept-notes.md — downstream planning should treat these as candidate requirements to be formalized, not pre-validated acceptance criteria.
